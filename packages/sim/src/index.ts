// @carburetor/sim — public entry point.
//
// v0.1: type contracts only. simulator implementations (combustor state
// machine, bus simulator, compute power model, ritual stage driver, browser
// playable, headless runner) are tracked in ROADMAP.md as v0.2 work.
//
// see ../../../docs/architecture.md for the five-layer architecture.
// see ../../../docs/codec-protocol.md for the telemetry wire format.

export * from './types';
