# 04-editions · pages 26-27

## page 26 (left, comparison table)

set in Plex Mono 9pt / 13pt leading. brass column rules between the three columns.

```
                    │  VAPORE       │  PROCESSTA       │  CLASSICA
────────────────────┼───────────────┼──────────────────┼────────────────────
cost                │  $0           │  ~$120 USD       │  ~$760 USD
time                │  5 minutes    │  one weekend     │  several weekends
parts               │  browser only │  esp32-s3 +      │  cox tee dee +
                    │               │   catalytic TEG +│   bg95 + sharp
                    │               │   LoRa + sharp   │   memory lcd + q10
                    │               │   lcd + ABS case │   + brass + canvas
─────────────────────────────────────────────────────────────────────────
radio               │  simulated    │  sx1262 LoRa     │  cat-m1 / nb-iot /
                    │               │   mesh           │   2g fallback
display             │  canvas       │  2.7" sharp      │  2.7" sharp memory
                    │               │   memory lcd     │   LCD (reflective)
keyboard            │  on-screen    │  5 tactile       │  blackberry q10
                    │               │   buttons        │
case                │  none         │  injection-      │  custom-sewn
                    │               │   molded ABS     │   brunswick green
                    │               │                 │   canvas + brass
─────────────────────────────────────────────────────────────────────────
target reader       │  curious      │  maker · student │  archival
                    │               │   · weekend      │   builder · long
                    │               │   builder        │   use
```

## page 27 (right, three short paragraphs)

```
VAPORE — the simulator

zero dollars. open a browser. pour fuel, prime, crank,
listen for the engine note, watch the supercap bus
climb, send a text, run out of fuel. vapore implements
all five layers in software; the telemetry frames it
produces are byte-identical to hardware. it is not a
toy. it is the architecture's first-class implementation.

PROCESSTA — the maker

~$120, one weekend, a quieter bill of materials.
an esp32-s3, an sx1262 LoRa radio, a sharp memory
lcd, five tactile buttons, a catalytic TEG power path,
a BQ25170 charger, and an injection-molded ABS case.
no cellular modem, no SIM slot, no calls. processta is
more honest with the "you cannot call on it" rule than
classica — it cannot even pretend.

CLASSICA — the definitive

~$760. several weekends. every part curated for ideal
behavior in its layer. the cox engine sits at the
project's spec; the sharp memory lcd is the canonical
low-power reflective display; the q10 keyboard is the
best thumb keyboard ever shipped. classica is closer
to a leica purchase than a smartphone purchase.
```

set in Plex Serif 10pt / 14pt. each paragraph's title in Plex Sans Bold 14pt brass.

## the small print at the bottom of page 27

```
all three editions implement the same five-layer
architecture and emit byte-identical telemetry. the
editions exist to validate the architecture's
swap-friendly claim. if you cannot build a $120 version
and a $760 version against the same interfaces, the
interfaces are not interfaces.
```

set in Plex Serif italic 9pt / 13pt, ~50mm wide centered block, with extra leading above it to separate from the three paragraphs.

`Q.E.D.`
